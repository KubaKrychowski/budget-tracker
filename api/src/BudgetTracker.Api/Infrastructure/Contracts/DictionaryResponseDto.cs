namespace BudgetTracker.Api.Infrastructure.Contracts;

/// <summary>Pozycja słownika w odpowiedzi API — sam kod; tekst dla użytkownika, jeśli potrzebny, składa front.</summary>
public sealed record DictionaryResponseDto(string Code);
